const chatSocketDocs =
    '## Connection and workflow\n\nUse socket.io-client against the backend origin (without /api/v1), default namespace and /socket.io path. Send the raw access JWT in auth.token (no Bearer prefix); query.token is also accepted. Missing/invalid tokens or missing profileId cause disconnect. The socket joins its profile-ID room automatically; managers also join role:manager.\n\nUser IDs identify message senders. Client/worker profile IDs identify recipients and memberships. Chat IDs identify both group and direct conversations.\n\nCleaning-plan lifecycle hooks create group chats, sync assigned workers, and deactivate groups. All managers have group access. Client-worker direct chats are created or reused by send-message; managers cannot start or access direct chats. There is no REST send-message endpoint.\n\n1. Fetch /chat/my-groups or /chat/my-direct-chats.\n2. Listen for events, then emit group:join for the selected chat.\n3. Load history from /chat-message/{chatId}.\n4. Send messages through Socket.IO. Re-fetch REST history after reconnect; these broadcasts are not a replay mechanism.\n5. De-duplicate messages by _id: the sender receives an acknowledgement and broadcasts may reach the same connection through multiple rooms.\n\n## Client-to-server events\n\n| Event | Payload | Result |\n| --- | --- | --- |\n| group:join | {groupId: chatId} | Checks membership, joins group:<chatId>; works for group and direct chats. No success acknowledgement. |\n| group:leave | {groupId: chatId} | Leaves room. No acknowledgement. |\n| group:send-message | {groupId: chatId, text?, attachments?} | Membership required; active chat required. Acknowledgement returns message. |\n| send-message | {receiver: oppositePartyProfileId, text?, attachments?} | Client-to-worker or worker-to-client only. Creates/reuses a direct chat. Acknowledgement returns message; data.chat is the chat ID. |\n| group:delete-message | {messageId} | Same rules as REST delete; supports both chat types. Acknowledgement returns soft-deleted message. |\n| group:typing | {groupId: chatId, name?} | Broadcasts typing to the room except sender. No acknowledgement. |\n| group:stop-typing | {groupId: chatId} | Broadcasts stop-typing to room except sender. No acknowledgement. |\n| seen | {chatId} | conversationId is accepted as a legacy alias. Intended for direct chats. No acknowledgement. |\n\nText or at least one attachment is required. Attachment shape: {url: string, type: "image" | "video" | "pdf" | "file"}. Send uploaded URLs, not file bytes. Sender identity and role come from the JWT.\n\nSend/delete acknowledgement: {success: true, data: ChatMessage}; failure: {success: false, message: string}, plus socket-error.\n\n## Server-to-client events\n\n| Event | Payload |\n| --- | --- |\n| group:new-message | ChatMessage with populated sender (_id, full_name, profile_photo, email) |\n| message:new | Same message shape, for direct chat |\n| group:message-deleted | {_id: messageId, chat: chatId} |\n| message:deleted | {_id: messageId, chat: chatId} |\n| group:renamed | {_id: chatId, name} |\n| group:added | {_id: chatId, name, cleaning_plan: planId} |\n| group:removed | {_id: chatId, cleaning_plan: planId} |\n| group:typing | {groupId, userId: senderUserId, name?} |\n| group:stop-typing | {groupId, userId: senderUserId} |\n| message:seen | {chatId}; emitted when unseen messages were updated in a direct chat |\n| onlineUser | Array of online profile IDs; broadcast on connection/disconnection |\n| socket-error | {code: number, message: string, type: "validation" | "database" | "auth" | "general" | "server", details?: any} |\n\nGroup message/delete/rename broadcasts go to the chat room, all managers and participant profile rooms. Direct-message broadcasts exclude the manager room. Membership added/removed events go to affected worker profile rooms.\n\nCurrent handler limitations: typing does not re-check membership; seen updates messages before checking chat type and does not verify membership. A removed worker is not forcibly removed from an already-joined room. These are current implementation limitations, not access guarantees. Presence tracks a set of profile IDs, not a connection count; disconnecting one tab can mark a multi-tab user offline.\n\n## Example\n\n```js\nimport { io } from "socket.io-client";\n\nconst socket = io(BACKEND_ORIGIN, { auth: { token: accessToken } });\nsocket.on("group:new-message", message => upsertMessage(message));\nsocket.on("message:new", message => upsertMessage(message));\nsocket.on("socket-error", error => console.error(error));\nsocket.on("connect", () => {\n  socket.emit("group:join", { groupId: chatId });\n});\nsocket.emit("group:send-message", {\n  groupId: chatId,\n  text: "Cleaning is complete",\n  attachments: []\n}, ack => {\n  if (ack.success) upsertMessage(ack.data);\n});\n// For direct chat:\n// socket.emit("send-message", { receiver: workerProfileId, text: "Hello" }, onAck);\n```\n\nSwagger Try it out executes HTTP requests only. Use a Socket.IO client for the events above.';

export default chatSocketDocs;
